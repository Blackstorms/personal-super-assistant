# Personal Super Assistant - Python backend
# 权威声明见 pyproject.toml；本文件供无 uv 环境的 pip install -r 使用。
# Hermes 可选 extras：pip install -e ".[hermes]" 或见下方 hermes 段。

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
openai>=1.55.0
httpx>=0.27.0
requests>=2.32.0
pyyaml>=6.0.2
python-multipart>=0.0.12
aiosqlite>=0.20.0
eval_type_backport>=0.2.0
python-dotenv>=1.0.0
mcp>=1.1.0; python_version >= "3.10"
pytest>=8.3.0
pytest-asyncio>=0.24.0
pypdf>=5.0.0
tiktoken>=0.7.0
sse-starlette>=2.1.0
croniter>=2.0.0
packaging>=24.0
ruff>=0.8.0

# 可选：更稳的 DuckDuckGo 客户端（未装则走 httpx HTML 回退）
# ddgs>=9.0.0

# Hermes bridge extras（vendored third_party/hermes-agent）
ruamel.yaml>=0.18.0
tenacity>=8.0.0
rich>=13.0.0
snowballstemmer>=2.0.0
